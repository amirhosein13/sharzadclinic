# بسته‌های آماده (موقت)

این شاخه فقط برای رساندن `node_modules` به سروری است که به مخزن npm
دسترسی ندارد. بعد از نصب پاک می‌شود — به تاریخچه‌ی اصلی پروژه ربطی ندارد.

    cat nm.part.* > nm.tar.gz
    sha256sum -c SHA256SUM
    tar -xzf nm.tar.gz
