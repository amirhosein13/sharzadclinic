# چک‌لیست راه‌اندازی

از بالا به پایین برو. هر بخش به بخش قبلی وابسته است.

---

## ۱. به‌روزرسانی پروژه روی لپ‌تاپ (۵ دقیقه)

```bash
cd ~/Desktop/mymomclinic/sharzadclinic
git pull
npm install                  # پکیج‌های جدید (پیامک، ایمیل، درایور SQL Server)
npx prisma migrate deploy    # ⚠️ حتماً — جدول‌های جدید ساخته می‌شوند
npx prisma generate
npm run dev
```

> بدون `migrate deploy` سایت بالا می‌آید ولی بخش‌های حقوق، پیامک و پرداخت
> خطا می‌دهند، چون جدول‌هایشان هنوز ساخته نشده.

**تست کن:**

| آدرس | چه چیزی را ببین |
|---|---|
| <http://localhost:3000> | صفحه‌ی اصلی |
| <http://localhost:3000/admin> | `admin@sharzadclinic.ir` / `Admin@12345` |
| <http://localhost:3000/login> | ورود مشتری — کد روی صفحه نشان داده می‌شود |

برای تست ورود مشتری، شماره‌ی `09121110001` را بزن (مشتری نمونه است).

---

## ۲. پر کردن محتوای واقعی کلینیک

همه‌ی این‌ها از پنل مدیریت انجام می‌شود، بدون دست‌زدن به کد.

- [ ] **تنظیمات** — نام کلینیک، تلفن، آدرس، اینستاگرام، مختصات نقشه، ساعات کاری
- [ ] **خدمات** — خدمات نمونه را با خدمات واقعی و قیمت‌های درست جایگزین کن
      (مدت هر جلسه و بافر بین نوبت‌ها را دقیق بزن — مبنای نوبت‌دهی همین است)
- [ ] **پرسنل** — همکاران واقعی، عکس، حقوق پایه، درصد پورسانت
- [ ] **برنامه‌ی هفتگی هر پرسنل** — بدون این، آن شخص در رزرو آنلاین نمایش داده نمی‌شود
- [ ] **گالری** — عکس‌های قبل و بعد (با رضایت کتبی مراجع)
- [ ] **نظرات** — نظرات واقعی مشتریان
- [ ] **کاربران** — برای منشی و هر اپراتور یک حساب جداگانه بساز
- [ ] **رضایت‌نامه‌ها** — سه متن نمونه از قبل هست؛ **حتماً** با نظر مسئول فنی
      کلینیک بازنویسی‌شان کن. متن هر رضایت‌نامه در لحظه‌ی امضا در سند قفل می‌شود

### عکس‌ها

همه‌ی تصاویر فعلی طرح جای‌گزین‌اند. دو راه:

1. **از پنل** — موقع ویرایش هر خدمت/پرسنل/نمونه‌کار، تصویر را آپلود کن (ساده‌تر)
2. **دستی** — فایل‌های `public/images/` را با همان نام جایگزین کن

---

## ۳. پیامک — بدون این، مشتری نمی‌تواند وارد شود

پنل پیامک بگیر (کاوه‌نگار پیشنهاد می‌شود) و در `.env` بنویس:

```env
SMS_PROVIDER="kavenegar"
KAVENEGAR_API_KEY="..."
KAVENEGAR_SENDER="..."
KAVENEGAR_OTP_TEMPLATE="نام الگوی تأییدشده برای کد ورود"
```

مراحل در پنل کاوه‌نگار:
1. خرید خط خدماتی
2. ثبت الگوی «کد ورود» در بخش **الگوها** و منتظر تأیید ماندن
3. کپی API Key از بخش تنظیمات

**تا وقتی این کار انجام نشده:** پیامک‌ها فقط در ترمینال چاپ می‌شوند. روی
لپ‌تاپ خودت مشکلی نیست، ولی اگر سایت را منتشر کنی مشتری‌ها نمی‌توانند
وارد حساب شوند (این را عمداً مسدود کرده‌ایم تا کد ورود لو نرود).

---

## ۴. مهاجرت دیتای اپلیکیشن قدیمی

نیاز است: آدرس سرور SQL Server، نام دیتابیس، یوزر و پسورد.

```bash
# ۱) اطلاعات اتصال را در .env بنویس (بخش LEGACY_MSSQL_*)
# ۲) ساختار دیتابیس قدیمی را ببین
npm run import:legacy -- --inspect

# ۳) بر اساس خروجی، فایل scripts/legacy-mapping.ts را اصلاح کن
#    (تنها فایلی است که باید دست بزنی)

# ۴) بدون نوشتن تست کن
npm run import:legacy -- --dry-run

# ۵) اجرای واقعی
npm run import:legacy
```

> خروجی مرحله‌ی `--inspect` را برای من بفرست تا نگاشت را برایت بنویسم.

اگر مبالغ در اپ قدیمی **ریال** بوده، در همان فایل `AMOUNT_DIVISOR` را روی `10` بگذار.

---

## ۵. قبل از انتشار سایت

- [ ] **`AUTH_SECRET` را عوض کن** — با `openssl rand -base64 32`
      (اگر این را عوض نکنی، هر کسی می‌تواند نشست جعلی بسازد)
- [ ] رمز عبور حساب `admin@sharzadclinic.ir` را از پنل → کاربران تغییر بده
- [ ] `NEXT_PUBLIC_SITE_URL` را روی دامنه‌ی واقعی بگذار
- [ ] پیامک را تنظیم کن (بخش ۳)
- [ ] یادآوری روزانه را با cron روی سرور فعال کن:
      ```
      0 10 * * *  cd /path/to/app && npm run reminders >> /var/log/sharzad.log 2>&1
      ```
- [ ] هاست را انتخاب کن (لیارا یا آروان پیشنهاد می‌شود؛ Vercel روی IP ایران بلاک است)

---

## ۶. اختیاری

### درگاه پرداخت (بیعانه‌ی آنلاین)

```env
ZARINPAL_MERCHANT_ID="شناسه‌ی پذیرنده"
ZARINPAL_SANDBOX="true"   # اول با این تست کن، بعد false کن
```

درصد بیعانه از **پنل → تنظیمات → درصد بیعانه‌ی آنلاین**.

### ایمیل

```env
SMTP_HOST="..."
SMTP_PORT="587"
SMTP_USER="..."
SMTP_PASS="..."
SMTP_FROM="کلینیک شهرزاد <no-reply@sharzadclinic.ir>"
```

---

## کاری که گیر کردی؟

- خطای `Cannot find native binding` → بخش «اگر به مشکل خوردی» در `README.md`
- خطای اتصال به دیتابیس → داکر بالا نیست، `docker compose up -d db` بزن
- سؤال دیگری بود، خروجی خطا را بفرست
